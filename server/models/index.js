const User = require('./User');
const Workspace = require('./Workspace');
const Message = require('./Message');
const Channel = require('./Channel');

module.exports = { User, Workspace, Message, Channel };
